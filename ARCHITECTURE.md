## 🏗️ Arquitetura do Sistema de Sessões

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          TELEGRAM BOT (Frontend)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  /sessions → [Menu Principal]                                              │
│             ├─ ➕ Nova Sessão → session_engine.create_session()            │
│             ├─ 📂 Minhas Sessões → session_engine.get_session_with_links() │
│             ├─ 🔄 Reutilizar → session_engine.reuse_session_before_start()│
│             └─ 📊 Dashboard → session_engine.get_session_with_links()      │
│                                                                             │
│  /start_session → Pede lista de entregadores → session_engine.start()     │
│  /session_dashboard → Mostra progresso real-time                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTP
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         FASTAPI BACKEND (Routes)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  POST   /api/sessions/create                                               │
│  POST   /api/sessions/{id}/packages                                        │
│  POST   /api/sessions/{id}/reuse                                           │
│  POST   /api/sessions/{id}/start                                           │
│  POST   /api/sessions/{id}/delivery/complete                               │
│  POST   /api/sessions/{id}/complete                                        │
│  GET    /api/sessions/{id}                                                 │
│  GET    /api/sessions/user/{id}                                            │
│  POST   /api/sessions/{id}/scan-barcode ← OCR para câmera quebrada         │
│  GET    /api/sessions/{id}/dashboard                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Business Logic
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    SESSION ENGINE (Core Logic)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ SessionEngine                                                        │  │
│  ├──────────────────────────────────────────────────────────────────────┤  │
│  │ • create_session()                                                   │  │
│  │ • add_packages_to_session()                                          │  │
│  │ • reuse_session_before_start()                                       │  │
│  │ • start_session()                                                    │  │
│  │ • mark_delivery_complete()                                           │  │
│  │ • complete_session()                                                 │  │
│  │ • get_session_with_links()                                           │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ BarcodeOCRService                                                    │  │
│  ├──────────────────────────────────────────────────────────────────────┤  │
│  │ Método 1: decode_with_zbar()        (95% confiança)                 │  │
│  │ Método 2: decode_with_tesseract()   (75% confiança)                 │  │
│  │ Método 3: decode_with_ml_matching() (80% confiança)                 │  │
│  │ Pipeline:  decode_barcode_full_pipeline() → Cascata automática      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Database Transactions
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    POSTGRESQL DATABASE (Persistence)                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────┐                                       │
│  │  delivery_sessions              │  Session principal                    │
│  ├─────────────────────────────────┤  • status (OPEN/ACTIVE/COMPLETED)    │
│  │ • session_id (PK)               │  • timestamps (created/started/etc)   │
│  │ • user_id (FK)                  │  • financial (cost/revenue/profit)    │
│  │ • status (ENUM)                 │  • was_reused, reuse_count            │
│  │ • financial_snapshot (JSON)     │  • 21 índices para performance        │
│  └─────────────────────────────────┘                                       │
│          │                                                                  │
│          ├──────────────────────────────────────────────────────┐           │
│          │                                                      │           │
│          ▼                                                      ▼           │
│  ┌──────────────────────┐                          ┌────────────────────┐  │
│  │ session_packages     │                          │ session_addresses  │  │
│  ├──────────────────────┤                          ├────────────────────┤  │
│  │ • package_id (PK)    │◄─────┐                   │ • address_id (PK)  │  │
│  │ • barcode            │      │ FK               │ • latitude/long    │  │
│  │ • delivery_status    │      │                   │ • geocoding_cache  │  │
│  │ • barcode_ocr_attempt│      │                   └────────────────────┘  │
│  │ • assigned_deliverer_id      │                                          │
│  └──────────────────────┘      │                                          │
│                        FK      │                                          │
│                        │       │                                          │
│                        ▼       │                                          │
│  ┌──────────────────────────────────────┐                                │
│  │  session_deliverers                  │                                │
│  ├──────────────────────────────────────┤                                │
│  │ • session_id + deliverer_id (unique) │                                │
│  │ • packages_assigned/delivered        │                                │
│  │ • base_salary, commission            │                                │
│  │ • total_earned (auto-calculado)      │                                │
│  │ • route_optimization (JSON)          │                                │
│  └──────────────────────────────────────┘                                │
│                                                                             │
│  ┌──────────────────────────────────────┐                                │
│  │  session_audit (Imutável)            │                                │
│  ├──────────────────────────────────────┤                                │
│  │ • session_id (FK)                    │                                │
│  │ • action (session_created, etc)      │                                │
│  │ • actor_id                           │                                │
│  │ • details (JSON)                     │                                │
│  │ • created_at (3 índices)             │                                │
│  └──────────────────────────────────────┘                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 📊 Fluxo de Dados (Exemplo Prático)

```
1. USUÁRIO CRIA SESSÃO
   /sessions → /sessions/create
   └─ SessionEngine.create_session(user_id=123)
      └─ INSERT delivery_sessions (status=OPEN)
         └─ INSERT session_audit (action=session_created)

2. USUÁRIO ADICIONA PACOTES
   /sessions/{id}/packages
   └─ SessionEngine.add_packages_to_session()
      ├─ INSERT session_addresses
      └─ INSERT session_packages (delivery_status=pending)

3. USUÁRIO INICIA DISTRIBUIÇÃO
   /sessions/{id}/start (deliverer_ids=[123, 456])
   └─ SessionEngine.start_session()
      ├─ UPDATE delivery_sessions (status=ACTIVE, started_at=now)
      ├─ INSERT session_deliverers (2 registros)
      └─ INSERT session_audit (action=session_started)

4. ENTREGADOR MARCA ENTREGA (Feedback do App/Telegram)
   /sessions/{id}/delivery/complete (package_id, deliverer_id)
   └─ SessionEngine.mark_delivery_complete()
      ├─ UPDATE session_packages (delivery_status=delivered)
      ├─ UPDATE session_deliverers (packages_delivered++, total_earned++)
      ├─ UPDATE delivery_sessions (total_revenue += package_value)
      └─ INSERT session_audit (action=package_delivered)

5. USUÁRIO FINALIZA SESSÃO
   /sessions/{id}/complete
   └─ SessionEngine.complete_session()
      ├─ UPDATE delivery_sessions (status=COMPLETED, is_readonly=True)
      ├─ JSON financial_snapshot (total_profit, pagamento, etc)
      └─ INSERT session_audit (action=session_completed)

6. CONSULTA HISTÓRICO (Read-only)
   /sessions/{id}
   └─ SessionEngine.get_session_with_links()
      └─ SELECT * FROM delivery_sessions + packages + deliverers + audit
         (Tudo linkado em uma resposta JSON completa)
```

## 🔄 Estados da Sessão

```
OPEN ──(add_packages)──> OPEN ──(start_session)──> ACTIVE ──(all delivered)──> COMPLETED ──(archive)──> ARCHIVED
│                          │                          │                         │                       │
├─ Pode reutilizar         ├─ Pode reutilizar        ├─ Entregas em progresso  ├─ Read-only           ├─ Histórico
├─ Sem duplicação          ├─ Sem duplicação         ├─ Real-time updates       ├─ financial_snapshot  ├─ Consultável
└─ Reuse rastreado         └─ Reuse rastreado        └─ Dashboard ativo        └─ Imutável            └─ Auditável
```

## 🎯 Resumo da Arquitetura

| Layer | Componente | Responsabilidade |
|-------|------------|------------------|
| **Telegram** | session_handlers.py | UX interativa, callbacks |
| **HTTP REST** | api_sessions.py | 11 endpoints, validação |
| **Business** | session_engine.py | Lógica de sessão, cálculos |
| **Vision** | barcode_ocr_service.py | OCR, decodificação |
| **Database** | schemas/sessions_schema.py | 5 tabelas, 80+ campos |
| **Migration** | alembic/versions/ | Versionamento de schema |

---

**Implementado por: Enzo 🔥 | Data: 31/01/2026**
